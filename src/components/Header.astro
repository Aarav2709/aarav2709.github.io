<header class="header fade-in">
  <div class="container">
    <h1 class="name">
      <span class="name-line">
        <span class="name-char">a</span><span class="name-char">a</span><span
          class="name-char">r</span
        ><span class="name-char">a</span><span class="name-char">v</span>
      </span>
      <span class="name-line">
        <span class="name-char">g</span><span class="name-char">u</span><span
          class="name-char">p</span
        ><span class="name-char">t</span><span class="name-char">a</span>
      </span>
    </h1>
    <p class="title">I am 15, I build full stack web apps, and I love clean products and dev tools :D</p>
    <div class="hero-links">
      <a
        href="https://mail.google.com/mail/?view=cm&fs=1&to=tribejustice35@gmail.com&su=Portfolio%20Inquiry&body=Hi%20Aarav%2C%0A%0A"
        target="_blank"
        rel="noopener"
      >mail</a>
      <a href="https://github.com/Aarav2709" target="_blank" rel="noopener">github</a>
      <a href="https://linkedin.com/in/aarav2709" target="_blank" rel="noopener">linkedin</a>
    </div>
  </div>
</header>

<button id="backToTop" class="back-to-top" aria-label="Back to top">
  <span class="back-to-top-icon">&uarr;</span>
</button>

<script>
  const backToTop = document.getElementById("backToTop");

  window.addEventListener(
    "scroll",
    () => {
      if (backToTop) {
        backToTop.classList.toggle("visible", window.pageYOffset > 300);
      }
    },
    { passive: true }
  );

  backToTop?.addEventListener("click", () => {
    window.scrollTo({ top: 0, behavior: "smooth" });
  });

  document.addEventListener("DOMContentLoaded", () => {
    const chars = document.querySelectorAll(".name-char");
    chars.forEach((char, i) => {
      (char as HTMLElement).style.animationDelay = `${i * 0.06}s`;
      char.classList.add("fade-in");
    });
  });
</script>

<style>
  .header {
    min-height: 82vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    position: relative;
    padding-top: 5.4rem;
  }

  .header .container {
    width: 100%;
    padding: 0;
  }

  .name {
    font-family: var(--font-display);
    font-size: var(--hero-size);
    font-weight: 400;
    letter-spacing: 0.04em;
    margin: 0;
    color: #d7d9af;
    line-height: 0.84;
    text-transform: lowercase;
    text-shadow: 0 2px 10px rgba(18, 24, 26, 0.3);
  }

  .name-line {
    display: block;
  }

  .name-line:first-child {
    margin-bottom: 0.05em;
  }

  .name-char {
    display: inline-block;
    margin-right: 0.03em;
    opacity: 0;
    animation: fadeIn 0.5s ease forwards;
  }

  .title {
    font-size: var(--font-size-large);
    color: var(--secondary-text);
    margin: 1.25rem 0 0;
    font-weight: 500;
    line-height: 1.72;
    max-width: 39rem;
    padding: 0;
  }

  .hero-links {
    display: flex;
    gap: 1rem;
    margin-top: 1.05rem;
    flex-wrap: wrap;
  }

  .hero-links a {
    display: inline-flex;
    align-items: center;
    color: #d6e8b1;
    font-size: var(--font-size-xs);
    font-weight: 500;
    font-family: var(--font-mono);
    letter-spacing: 0.05em;
    text-decoration: none;
    text-transform: lowercase;
    padding-bottom: 0.12rem;
    border-bottom: 1px solid rgba(167, 192, 128, 0.4);
    transition: var(--transition-smooth);
  }

  .hero-links a:hover {
    color: #ecf8ca;
    border-bottom-color: rgba(223, 242, 180, 0.74);
    transform: translateY(-1px);
    text-decoration: none;
    opacity: 1;
  }

  .back-to-top {
    position: fixed;
    bottom: 1.15rem;
    right: 1.15rem;
    width: 2.35rem;
    height: 2.35rem;
    background: rgba(43, 51, 57, 0.86);
    color: var(--primary-text);
    border: 1px solid rgba(167, 192, 128, 0.4);
    border-radius: 999px;
    font-size: 0.95rem;
    cursor: pointer;
    transition: var(--transition-smooth);
    z-index: 1100;
    opacity: 0;
    visibility: hidden;
    transform: translateY(10px);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow);
  }

  .back-to-top.visible {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .back-to-top:hover {
    background: rgba(167, 192, 128, 0.24);
    color: #e5f3bf;
    border-color: rgba(181, 214, 145, 0.6);
    transform: translateY(-2px);
    opacity: 1;
  }

  @media (max-width: 768px) {
    .header {
      min-height: 70vh;
      padding-top: 4.6rem;
    }

    .header .container {
      padding: 0;
    }

    .title {
      margin-top: 1.1rem;
      font-size: var(--font-size-base);
    }

    .hero-links {
      gap: 0.8rem;
    }
  }
</style>
